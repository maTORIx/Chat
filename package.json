{
  "dependencies": {
    "@types/react": "^18.2.35",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "start": "npm-run-all --parallel watch server",
    "build": "bun build webapp/script.tsx --outdir build --minify",
    "watch": "bun build webapp/script.tsx --outdir build --watch",
    "server": "python server.py"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}